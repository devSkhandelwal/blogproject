import React from 'react'

const Input = () => {

    const inputElement = element === "input" ?(
                         <input /> 
                    ): "textarea" ? (
                         <textarea/> 
                    ):null

    return (
        <div>
            
        </div>
    )
}

export default Input
