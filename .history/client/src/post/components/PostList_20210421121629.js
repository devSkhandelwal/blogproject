import React from 'react'

const PostList = ({posts}) => {
    return (
            <div>
        {  
              posts.map(post=>{

            })
        }
        </div>
    )
}

export default PostList
